# 心理健康智能平台 - 启动指南

## 快速启动

### 方法1：使用启动脚本（推荐）

**Windows批处理：**
```bash
双击运行 start.bat
```

**PowerShell：**
```powershell
.\start.ps1
```

### 方法2：手动启动

#### 步骤1：启动后端服务

打开终端1，执行：
```bash
cd mental-health-backend
npm install  # 如果还没安装依赖
npm run dev
```

后端服务将在 `http://localhost:8000` 启动

#### 步骤2：启动前端服务

打开终端2，执行：
```bash
cd mental-health-frontend
npm install  # 如果还没安装依赖
npm run dev
```

前端服务将在 `http://localhost:63334` 启动

## 访问地址

- **前端应用**: http://localhost:63334
- **后端API**: http://localhost:8000

## 常见问题解决

### 1. 端口被占用

如果端口被占用，可以：

**修改前端端口：**
编辑 `mental-health-frontend/vite.config.js`，修改 `port` 值：
```javascript
server: {
  port: 63335,  // 改为其他端口
  // ...
}
```

**修改后端端口：**
编辑 `mental-health-backend/server.js`，修改 `PORT` 值：
```javascript
const PORT = process.env.PORT || 8001;  // 改为其他端口
```

### 2. 依赖未安装

如果遇到模块找不到的错误，执行：
```bash
# 后端
cd mental-health-backend
npm install

# 前端
cd mental-health-frontend
npm install
```

### 3. 服务无法访问

1. 检查服务是否正在运行（查看终端输出）
2. 检查防火墙设置
3. 尝试使用 `127.0.0.1` 代替 `localhost`
4. 检查浏览器控制台是否有错误信息

### 4. 网页显示空白

1. 打开浏览器开发者工具（F12）
2. 查看 Console 标签页的错误信息
3. 查看 Network 标签页，检查API请求是否成功
4. 确保后端服务正在运行

### 5. API请求失败

1. 确保后端服务在 `http://localhost:8000` 运行
2. 检查 `vite.config.js` 中的代理配置是否正确
3. 检查浏览器控制台的网络请求错误

## 验证服务状态

### 检查后端服务
在浏览器访问：http://localhost:8000

应该看到：
```json
{
  "message": "心理健康智能平台API",
  "version": "1.0.0"
}
```

### 检查前端服务
在浏览器访问：http://localhost:63334

应该看到心理健康智能平台的首页

## 开发模式

- 前端支持热重载，修改代码后自动刷新
- 后端需要手动重启才能看到更改

## 停止服务

在运行服务的终端窗口中按 `Ctrl + C` 停止服务

